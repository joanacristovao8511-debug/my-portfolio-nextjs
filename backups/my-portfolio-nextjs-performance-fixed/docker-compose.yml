services:
  portfolio:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      DATABASE_URL: "file:/app/data/portfolio.db"
    volumes:
      - portfolio_data:/app/data
    restart: unless-stopped

volumes:
  portfolio_data:
